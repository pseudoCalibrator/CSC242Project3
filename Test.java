import bn.*;
import parser.*;

public class Test {
    public static void main(Sting[] args){
        // If the first argument is not a number, we're running exact inferencer
        if(!Character.isDigit(args[0].charAt(0))){
            // parse xml file
            if(args[0].endsWith("xml")){

            }
            // parse bif
            else{

            }
        }
        // If the first argument is a number, we're either doing reject or weight approximate
        else{
            // parse xml file
            if(args[0].endsWith("xml")){

            }
            // parse bif
            else{

            }
        }
    }
}
