import bn.*;
import parser.*;

public class Test {
    public static void main(Sting[] args){
        // If the first argument is a number, we're running approximate inferencers
        if(Character.isDigit(args[0].charAt(0))){

        }
    }
}
